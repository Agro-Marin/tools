[tool:pytest]
# Configuración pytest para field_method_detector
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = 
    --tb=short
    -v
    --disable-warnings
markers =
    mvp: Tests del MVP minimalista
    unit: Tests unitarios
    integration: Tests de integración